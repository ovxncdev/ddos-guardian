/**
 * Utilities Index
 * 
 * Usage:
 *   const { environment, paths } = require('./utils');
 */

const environment = require('./environment');
const paths = require('./paths');

module.exports = {
    environment,
    paths,
};
